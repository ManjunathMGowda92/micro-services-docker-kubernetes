spring:
  application:
    name: accounts
  config:
    import:
      - "optional:configserver:http://localhost:8071/"

app:
  details:
    name: accounts-service
    author: Manjunath
    description: Accounts Application
    version: "2.0.0"
    environment: "LOCAL"
