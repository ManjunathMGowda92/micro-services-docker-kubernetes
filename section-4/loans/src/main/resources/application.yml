spring:
  application:
    name: loans
  config:
    import:
      - "optional:configserver:http://localhost:8071/"
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "fourstack"
    password: "stack4@2808"

  datasource:
    url: jdbc:postgresql://localhost:5432/loans
    driver-class-name: org.postgresql.Driver
    username: fourstack
    password: 'BankDbpSql@0109!'
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 9000
  servlet:
    context-path: /loans-service

app:
  details:
    name: loans-service
    author: Manjunath
    description: Loans Application
    version: "2.0.0"
    environment: "DEFAULT"

loans:
  message: "Welcome to Loans service"
  contactDetails:
    name: "Manjunath"
    email: "man1234@gmail.com"
  onCallSupport:
    - (001)111-1234
    - (001)111-1235

management:
  endpoints:
    web:
      exposure:
        include:
          - "busrefresh"
